/* eslint-disable no-unused-vars */
export default style => node => {
    if (!node || !node.type) return '';
    switch (node.type) {
        case 'tag':
            return '';
        case 'text':
            return node.text || '';
        default:
    }
    return '';
};
