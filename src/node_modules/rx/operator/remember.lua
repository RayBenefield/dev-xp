local observable = require('rx.observable')
local observer = require('rx.observer')

return function()
    return function(source)
        return observable(source.subscribe, source, true)
    end
end
