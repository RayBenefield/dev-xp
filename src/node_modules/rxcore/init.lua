local extendRx = require('rx')
local fromEvent = require('rxcore.observable.fromEvent')

local extension = function()
    local rx = {
        fromEvent = fromEvent,
    }

    return rx
end

return function()
    return extendRx(extension)
end
