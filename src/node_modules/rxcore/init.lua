local rx = require('rx')
local of = require('rx.observable.of')

local extension = function()
    local rx = {
        fromEvent = function(event) return of(event) end
    }

    return rx
end

return function()
    return rx(extension)
end
