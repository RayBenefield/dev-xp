local observable = require('rx.observable')
local observer = require('rx.observer')

return function(eventName)
    return observable(function(destination)
        local next = function(...)
            destination.next(...)
        end

        Events.Connect(
            eventName,
            observer(destination, next)
        )
    end)
end
