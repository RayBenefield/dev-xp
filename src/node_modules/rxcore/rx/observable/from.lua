local create = require('rx.observable.create')

return function(table, keys) return create(
    function(subscriber)
        for key, value in pairs(table) do
            if (not keys) then
                subscriber.next(value)
            else
                subscriber.next(value, key)
            end
        end

        subscriber.complete()
    end)
end
