local create = require('rx.observable.create')

return function(start, last, increment)
    increment = increment or 1

    return create(function(destination)
        local next = start

        while (next ~= last) do
            destination.next(next)
            next = next + increment
        end
        destination.next(next)

        destination.complete()
    end)
end
