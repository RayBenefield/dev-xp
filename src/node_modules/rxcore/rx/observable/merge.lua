local util = require('util')
local from = require('rx.observable.from')
local mergeMap = require('rx.operators.mergeMap')

return function(...)
    local sources = from({...})
    return mergeMap(util.identity)(sources)
end
