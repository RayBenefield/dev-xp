local create = require('rx.observable.create')

return function(delay) return create(function(subscriber)
    local number = 0

    Task.Wait(delay)
    subscriber.next(number)

    subscriber.complete()
end)
end
