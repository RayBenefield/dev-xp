local observable = require('rx.observable')
local operators = require('rx.operators')

local rx = {
    observable = observable,
    operators = operators,
}

return function() return rx, operators end
