local operate = require('rx.operators.operate')
local createObserver = require('rx.observer.create')

--TODO
-- Add print table and label functionality back
return function()
    return operate(function(source, destination)
        source.subscribe(
            createObserver(
                destination,
                function(...)
                    print(...)
                    destination.next(...)
                end
            )
        )
    end)
end
