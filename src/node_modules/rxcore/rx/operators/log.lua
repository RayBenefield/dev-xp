local create = require('rx.observable.create')

return function()
    return function(source, destination)
        source.subscribe(
            function(...)
                print(...)
                destination.next(...)
            end,
            destination.error,
            destination.complete
        )
        return function()
            destination.complete()
        end
    end
end
