import describe from 'kape'
import sanitize from '@rampant-wit/sanitize'

describe('Sanitize Message', sanitize, snapshot => {
    snapshot('Lowercase', [['Capital to Lowercase']])
    snapshot('Punctuation', [["Remove punctuation like I'm"]])
    snapshot('Trim', [[' Trim space on both sides ']])
    snapshot('Collapse Whitespace', [
        ['Collapse  multiple   spaces  to     one'],
    ])
})
