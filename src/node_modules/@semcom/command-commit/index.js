import transmute from 'transmutation'
import gitStatus from '@semcom/git-status'

export default function status() {
    return transmute({}).extend('status', gitStatus)
}

export const command = () => ({
    name: 'commit',
    aliases: ['ci'],
    run: status,
})
