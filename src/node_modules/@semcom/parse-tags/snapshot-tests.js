import describe from 'kape';
import parseTags from '@semcom/parse-tags';

describe('Tag Parser', parseTags, it => {
    it(given =>
        given([
            [],
            [{ rawTags: [] }],
            [{ rawTags: ['#1'] }],
            [{ rawTags: ['#1', 'roar'] }],
        ]),
    );
});
