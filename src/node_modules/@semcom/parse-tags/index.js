const regex = /#(\d*)/

export default ({ rawTags = [] } = {}) =>
    rawTags
        .map(tag => tag.match(regex) || [])
        .filter(matches => matches.length)
        .map(([tag, releaseNumber]) => ({
            tag,
            releaseNumber: parseInt(releaseNumber, 10),
        }))
