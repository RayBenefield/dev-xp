export default ({ plugins = {} } = {}) => ({ type, name, checks }) => {
    const noFixes = { fixable: [], fixes: {} }
    if (!type) return noFixes
    if (!plugins[type]) return noFixes
    if (!plugins[type].fix) return noFixes

    return plugins[type].fix({ name, checks })
}
