import * as R from 'ramda'
import log from '@dev-xp/log'
import { Package } from 'node-wick'

const getData = R.invoker(0, 'get_data')
const pkg = R.constructN(1, Package)

const wick = { parse: R.pipe(pkg, getData) }

export default wick
