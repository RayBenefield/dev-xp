import describe from 'kape'
import checkPackage from '@entyre/check-package'

describe('Entyre Check Package', checkPackage, snapshot => {
    snapshot(
        [],
        [{ structure: { files: [] } }],
        [{ structure: { files: ['index.md'] } }],
        [{ manifest: { version: '0.0.1' } }],
        [
            {
                structure: { files: ['index.md'] },
                manifest: { version: '0.0.1' },
            },
        ]
    )
})
