export default async ({
    structure: { files = [] } = {},
    manifest: { version } = {},
} = {}) => {
    const checks = {
        'has a version number': !!version,
        'has index.md': files.includes('index.md'),
    }

    return { ...checks, publishable: Object.values(checks).every(pass => pass) }
}
