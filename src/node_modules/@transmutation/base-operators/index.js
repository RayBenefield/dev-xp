import log from '@transmutation/logging-operator'
import extendEach from '@transmutation/extend-each'
import under from '@transmutation/umbrella-operator'
import extend from '@transmutation/extension-operator'
import ifOperator from '@transmutation/existence-operator'
import doOperator from '@transmutation/side-effect-operator'
import ifNo from '@transmutation/existence-inverse-operator'
import switchOperator from '@transmutation/selector-operator'
import doEach from '@transmutation/side-effect-iterator-operator'

export default {
    extend,
    extendEach,
    do: doOperator,
    doEach,
    if: ifOperator,
    ifNo,
    log,
    switch: switchOperator,
    under,
}
