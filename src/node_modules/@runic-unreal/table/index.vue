<template>
    <div class='background'></div>
</template>

<style lang="sass" scoped>
$grid-size: 170px
$grid-color: #333333
$bg-color: #262626
$dark-color: #1c1c1c
$th: 0.008

@function line($dir, $t, $w, $color)
    $start: $grid-size * $t - $w * $grid-size
    $end: $grid-size * $t + $w * $grid-size
    @return linear-gradient(to $dir, transparent $start, $color $start, $color $end, transparent $end)

@function lines($dir)
    @return line($dir, 0.125, $th, $grid-color), line($dir, 0.25, $th, $grid-color), line($dir, 0.375, $th, $grid-color), line($dir, 0.5, $th, $grid-color), line($dir, 0.625, $th, $grid-color), line($dir, 0.75, $th, $grid-color), line($dir, 0.875, $th, $grid-color), line($dir, 1, $th * 2, $grid-color)

.background
    z-index: -5
    background-size: $grid-size $grid-size
    background-image: line(right, 0.5, $th, $dark-color), line(bottom, 0.5, $th, $dark-color), lines(right), lines(bottom)
    background-color: $bg-color
    box-sizing: border-box
    background-repeat: repeat
</style>
