import operators from '@transmutation/base-operators'
import isolator from '@transmutation/isolation-function'
import configure from '@transmutation/configure-transmuter'

const transmuter = configure(operators)
Object.assign(transmuter, operators)

export default transmuter
export const configureTransmuter = configure
export const transmute = transmuter
export const isolate = isolator
