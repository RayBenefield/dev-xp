export default ({ searcher }) => ({ request, defaultCommand }) => {
    if (!request) return defaultCommand

    const results = searcher.get(request)
    if (results.length > 0) return results[0]

    return defaultCommand
}
