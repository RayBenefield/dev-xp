export default ({ searcher }) => args => {
    const command = searcher.get(args[0])[0];
    const result = command.run();
    if (!result) return true;
    if (typeof result.then === 'function') {
        return result.then();
    }
    return false;
};
