export default cleansings => args =>
    Object.entries(args)
        .map(
            ([name, value]) =>
                name in cleansings
                    ? { [name]: cleansings[name](value) }
                    : { [name]: value }
        )
        .reduce((all, arg) => ({ ...all, ...arg }), {})
