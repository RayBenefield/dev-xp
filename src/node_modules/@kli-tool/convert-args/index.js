export default (args, conversion = {}) =>
    Object.entries(args)
        .map(
            ([key, value]) =>
                key in conversion ? [conversion[key], value] : [key, value],
        )
        .reduce((obj, pair) => ({ ...obj, [pair[0]]: pair[1] }), {});
