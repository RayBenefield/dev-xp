import rx from 'xstream'

const test = rx.create({
    start: () => {},
    stop: () => {},
})

test.subscribe({ next: console.log })
test.subscribe({ next: console.log })

test.shamefullySendNext(Date.now())
