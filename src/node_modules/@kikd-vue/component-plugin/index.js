import getJsDetails from '@kikd/package-details-js'

const type = 'vue-component'

export default {
    type,
    details: () => ({ main: 'index.vue' }),
    filter: ({ files = [] }) => files.includes('index.vue'),
    manifest: getJsDetails,
}
