import describe from 'kape';
import gather from '@kikd/change-gatherer';

describe('Change Gatherer', gather, it => {
    it('snapshot', given =>
        given([
            // [{ from: 'kikd-v0.0.65' }],
            // [{ to: 'v0.0.3' }],
            [{ from: 'v0.0.1', to: 'v0.0.2' }],
            [{ from: 'v0.0.1', to: 'v0.0.3' }],
            [{ from: 'v0.0.2', to: 'v0.0.3' }],
        ]));
});
