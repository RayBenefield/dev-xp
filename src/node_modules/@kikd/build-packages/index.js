export default ({ packages = [] } = {}) =>
    new Promise(res =>
        res(
            packages
                .filter(p => p.bin)
                .filter(p => p.bin.constructor === Object),
        ),
    );
