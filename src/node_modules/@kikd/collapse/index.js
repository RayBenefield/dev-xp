export default (all, curr) => {
    if (curr.undefined) {
        const und = all.undefined
            ? [...all.undefined, curr.undefined]
            : [curr.undefined];
        return {
            ...all,
            undefined: und,
        };
    }
    return Object.assign(all, curr);
};
