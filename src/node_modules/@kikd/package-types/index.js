import log from '@dev-xp/log'
import * as _ from '@dev-xp/utils'

export default ({ plugAll }) => ({ structures = {} }) => {
    const getFirstTrue = _.pipe(
        plugAll('filter'),
        _.filter(_.isTrue),
        _.keys,
        _.head
    )
    return _.map(getFirstTrue, structures)
}
