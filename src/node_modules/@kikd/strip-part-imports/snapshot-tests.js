import describe from 'kape'
import strip from '@kikd/strip-part-imports'

describe('Strip Part Imports', strip, snapshot => {
    snapshot('Empty Lists', [[], [[]]])
    snapshot('Word Packages', [[['argv']], [['inquirer']]])
    snapshot('With Dashes', [[['javascript-stringify']], [['word-wrap']]])
    snapshot('With Dots', [[['lodash.defaults']], [['lodash.unique']]])
    snapshot('Namespaced Packages', [
        [['@mixer/client-node']],
        [['@babel/plugins']],
    ])
    snapshot('Part Imports', [
        [['simple-git/promise']],
        [['loud-rejection/register']],
    ])
    snapshot('Namespaced With Parts', [
        [['@mixer/client-node/chat']],
        [['@babel/plugins/star-imports']],
    ])
})
