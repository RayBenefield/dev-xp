import getJsDetails from '@kikd/package-details-js'
import getStructures from '@kikd/package-structure'

export default ({ root = '.', packageNames = [] }) =>
    packageNames.map(pkg => getStructures({ root, name: pkg })).map(pkg => ({
        ...pkg,
        ...getJsDetails({ root, ...pkg }),
    }))
