import getPackages from '@kikd/command-list';
import publishCheck from '@kikd/publish-check';
import template from '@kikd/template-checked-packages';

export default function check(...args) {
    return getPackages(...args).extend('checks', publishCheck);
}

export const command = {
    name: 'check',
    run: check,
    template,
};
