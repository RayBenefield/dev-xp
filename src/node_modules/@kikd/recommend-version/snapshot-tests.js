import describe from 'kape';
import recommendVersion from '@kikd/recommend-version';

describe('Recommend Version', recommendVersion, it => {
    it('should capture snapshot', given => given([[]]));
});
