import { dirname } from 'path'

import findUp from 'find-up'

export const sync = ({ anchor = '.git' } = {}) => dirname(findUp.sync(anchor))

export default ({ anchor = '.git' } = {}) => findUp(anchor).then(dirname)
