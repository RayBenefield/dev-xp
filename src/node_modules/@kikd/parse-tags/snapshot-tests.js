import describe from 'kape';
import parseTags from '@kikd/parse-tags';

describe('Tag Parser', parseTags, it => {
    it('snapshot', given =>
        given([
            [],
            [{ rawTags: [] }],
            [{ rawTags: ['#1'] }],
            [{ rawTags: ['#1', 'roar'] }],
        ]));
});
