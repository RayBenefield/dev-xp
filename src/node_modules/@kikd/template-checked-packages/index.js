export default packages => `
    <bar>-</bar>
    <h1>Checked Packages</h1>
    <bar>-</bar>
    <list>
        ${packages
            .map(
                pkg => `<item>
                <title>${pkg.name}</title>
                <success>✔</success> <info>publishable</info>
            </item>`,
            )
            .join('')}
    </list>
    `;
