export default ({ packages = [] } = {}) =>
    new Promise(res => {
        if (packages.length === 0) return res([]);
        const results = packages.map(pkg => ({
            ...pkg,
            publishable: false,
        }));
        return res(results);
    });
