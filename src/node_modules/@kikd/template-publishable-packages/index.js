export default ({ publishable }) => `
    <bar>-</bar>
    <h1>Publishable Packages</h1>
    <bar>-</bar>
    <list>
        ${publishable
            .map(
                ({ name }) => `<item>
                    <check><success>${name}</success></check>
                </item>`,
            )
            .join('')}
    </list>
`;
