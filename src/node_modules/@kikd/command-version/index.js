import getPublishablePackages from '@kikd/command-publish';
import recommendVersions from '@kikd/recommend-version';

export default (...args) =>
    getPublishablePackages(...args).extend('updates', snowball =>
        recommendVersions({ ...snowball, packages: snowball.publishable }),
    );
