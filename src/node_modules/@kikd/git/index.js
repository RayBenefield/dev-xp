import configureGit from 'simple-git/promise'

export const silent = configureGit()

export default configureGit().outputHandler((command, stdout, stderr) => {
    stdout.pipe(process.stdout)
    stderr.pipe(process.stderr)
})
