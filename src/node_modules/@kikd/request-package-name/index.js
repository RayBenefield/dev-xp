import { prompt } from 'inquirer'

export default ({ packageName }) => () =>
    prompt([
        {
            type: 'input',
            name: 'name',
            default: packageName[0],
            message: 'What is the name of this new package?',
        },
    ]).then(({ name }) => name)
