import { prompt } from 'inquirer'

export default ({ packageNames = [] } = {}) => () =>
    prompt([
        {
            type: 'input',
            name: 'name',
            message: 'What is the name of this new package?',
            default: packageNames[0] || null,
        },
    ]).then(({ name }) => name)
