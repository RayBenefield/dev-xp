import describe from 'kape'
import { resolve } from 'path'
import detect from '@kikd/dependency-detective'

describe(
    'Dependency Detective',
    detect(resolve(__dirname, 'fixtures/src/node_modules')),
    snapshot =>
        snapshot(
            [],
            ['no-config'],
            ['kikd'],
            ['@kikd/first'],
            ['@kikd/second'],
            ['@kikd/third']
        )
)
