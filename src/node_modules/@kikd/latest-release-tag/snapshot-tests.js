import describe from 'kape';
import latestReleaseTag from '@kikd/latest-release-tag';

describe('Latest Release Tag', latestReleaseTag, it => {
    it('snapshot', given =>
        given([
            [],
            [{ tags: [] }],
            [{ tags: [{ random: 'stuff' }] }],
            [
                {
                    tags: [
                        {
                            tag: '#1(PR-248)',
                            releaseNumber: '1',
                            pullRequestNumber: '248',
                        },
                    ],
                },
            ],
        ]));
});
