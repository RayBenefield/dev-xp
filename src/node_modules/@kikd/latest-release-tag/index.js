export default ({ tags = [] } = {}) =>
    tags
        .filter(tag => tag.releaseNumber)
        .reduce(
            (latest, current) =>
                latest.releaseNumber > current.releaseNumber ? latest : current,
            { releaseNumber: 0 },
        ).tag || 'HEAD';
