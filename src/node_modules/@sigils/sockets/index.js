import { Socket } from 'rete'

const number = new Socket('number')
const fn = new Socket('function')

const any = new Socket('any')
any.combineWith(number)
any.combineWith(fn)

export default { any, number, function: fn }
