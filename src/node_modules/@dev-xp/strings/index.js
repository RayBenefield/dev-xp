/* eslint-disable import/prefer-default-export, no-unused-vars */
import _ from 'chalk'
import line from 'singleline'

export const noPlug = (name, fn) => {
    const $ = `The [${name}] tool does not support a [${fn}] plug.`
    return _.red($)
}
