const diminishingReturns = (val, scale) => {
    if (val < 0) return -diminishingReturns(-val, scale)

    const mult = val / scale
    const trinum = (Math.sqrt(8.0 * mult + 1.0) - 1.0) / 2.0
    return trinum * scale
}

export default diminishingReturns
