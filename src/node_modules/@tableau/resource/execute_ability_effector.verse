using. Tableau

execute_ability_effector<public> := class(effector):
    GameState<public>:game_state

    Play<override>(Source:string, Activator:string, Focus:prop_path, Pay:load)<suspends>:?[]command=
        GameState.GetAbilityExecPay(Focus, Activator)(Pay)
        return option. array{}
