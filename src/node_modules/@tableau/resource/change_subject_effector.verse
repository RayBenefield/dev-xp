using { Tableau }

change_subject_effector<public> := class(effector):
    GameState<public>:game_state

    Play<override>(Command:command, Payload:payload):command=
        Subject := Payload.Str
        return command:
            CopyCommand<constructor>(Command)
            Subject := Subject
