
using. /Verse.org/Simulation
using. /Fortnite.com/Devices

dice_device_v0_1<public> := class(tableau_device):
    @editable ResourceID<override>:string = "Dice"
    @editable TriggerSource:prop_path = prop_path{}
    @editable Sides:int = 2

    GetResources<override>():[]resource= array. resource:
        ID := ResourceID
        Props := map:
            "Sides" => Integer of Sides
            "Value" => Integer of (Path(TriggerSource), "Roll", Path("Sides"))
