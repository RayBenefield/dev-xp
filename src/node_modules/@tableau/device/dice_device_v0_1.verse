
using. /Verse.org/Simulation

dice_device_v0_1<public> := class(tableau_device):
    DeviceType<override>:string = "DICE"

    @editable Trigger:prop_path = prop_path{}
    @editable Sides:int_plug = int_plug. Default := 2

    GetResources<override>():[]resource= array. resource:
        Props := map:
            "Sides" => Integer of Sides
            "Value" => Integer of (Path(Trigger), "Roll", Path("Sides"))
