
using. /Verse.org/Simulation
using. /Fortnite.com/Devices

billboard_device_v0_1<public> := class(tableau_device):
    DeviceType<override>:string = "BILLBOARD"

    @editable Board:billboard_device = billboard_device{}
    @editable Text<public>:str_plug = str_plug{}

    GetResources<override>():[]resource= array. resource:
        Props := map:
            "Text" => Str of Text

    GetSideEffects<override>()<suspends>:[]side_effect= array:
        PushMsg("Text", SetText(Board).FN)
