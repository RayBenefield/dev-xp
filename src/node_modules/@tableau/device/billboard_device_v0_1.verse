
using. /Verse.org/Simulation
using. /Fortnite.com/Devices

using. Tableau

billboard_device_v0_1<public> := class(tableau_device):
    @editable Board:billboard_device = billboard_device{}
    @editable TextSource<public>:prop_path = prop_path{}

    GetSideEffects<override>(State:scoped_state)<suspends>:[]side_effect= array:
        PushMsg(TextSource, SetText(Board).FN)
