
using. /Verse.org/Simulation
using. /Fortnite.com/Devices

iterator_device_v0_1<public> := class(tableau_device):
    @editable ResourceID<override>:string = "Iterator"
    @editable StartingValue:int = 1

    GetResources<override>():[]resource= array. resource:
        ID := ResourceID
        Props := map:
            "Zero Based Value" => Integer of (Path("Value"), "-", ONE())
            "Value" => Integer of StartingValue
            "Starting Value" => Integer of StartingValue
        Actions := map:
            "Increment" => Action of Increase("Value").By(ONE())
            "Decrement" => Action of Decrease("Value").By(ONE())
            "Reset" => Action of Set("Value").To("Starting Value")
