
using. /Verse.org/Simulation
using. /Fortnite.com/Devices

iterator_device_v0_1<public> := class(tableau_device):
    @editable ResourceID<override>:string = "Iterator"
    @editable StartingValue:int = 0

    GetResources<override>():[]resource= array. resource:
        ID := ResourceID
        Props := map:
            "Zero Based Value" => Integer of StartingValue
            "Value" => Integer of (Path("Zero Based Value"), "+", ONE())
        Actions := map:
            "Increment" => Action of Increase("Zero Based Value").By(ONE())
            "Decrement" => Action of Decrease("Zero Based Value").By(ONE())
            "Reset" => Action of Set("Zero Based Value").By(ZERO())
