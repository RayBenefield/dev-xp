
using. /Fortnite.com/Devices
using. /Verse.org/Simulation
using. /Fortnite.com/Characters

player_device := class(creative_device):
    @editable Spawner:player_spawner_device = player_spawner_device{}

    OnBegin<override>()<suspends>:void=
        Spawner.SpawnedEvent.Subscribe(HidePlayer)

    HidePlayer(Agent:agent):void=
        if (Char := Agent.GetFortCharacter[]):
            StasisArgs := stasis_args:
                AllowTurning := false
                AllowFalling := false
                AllowEmotes := false
            Char.PutInStasis(StasisArgs)
            Char.Hide()
