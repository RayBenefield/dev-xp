
using. /Verse.org/Simulation

using. Numb

currency_device_v0_1<public> := class(tableau_device):
    DeviceType<override>:string = "CURRENCY"

    @editable Starting:num_plug = num_plug{}

    GetResources<override>():[]resource= array. resource:
        Props := map:
            "Starting" => Num of Starting
            "Count" => Num of Starting.Default
        Actions := map:
            "Decrease" => Action of Decrease("Count").By(TWEAK())
            "Increase" => Action of Increase("Count").By(TWEAK())
