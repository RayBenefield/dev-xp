
using. /Verse.org/Simulation
using. /Fortnite.com/Devices

using. Vertex

bundle_remap_v0_1 := class<concrete>():
    @editable TargetAction:string = "New Action"
    @editable SourceAction:prop_path = prop_path{}

bundle_device_v0_1<public> := class(tableau_device):
    DeviceType<override>:string = "BUNDLE"

    @editable Actions:[]bundle_remap_v0_1 = array{}

    GetResources<override>():[]resource= array. resource:
        Actions := CreateMap of for(AID:Actions). (AID.TargetAction, Action of AID.SourceAction)
