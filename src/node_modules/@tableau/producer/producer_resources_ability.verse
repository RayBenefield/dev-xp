using { Tableau }
using { TableauParticipant }

command_maker<public> := class():
    AfterEffects<public>:[]effect

    Make<public>(Subject:string, Target:string, Payload:payload):command=
        command:
            Subject := Subject
            Target := Target
            Payload := Payload
            AfterEffects := AfterEffects
