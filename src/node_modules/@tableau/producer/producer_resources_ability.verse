using { Tableau }
using { TableauParticipant }

command_maker<public> := class():
    AfterEffects<public>:[]effect

    Make<public>(Subject:string, Target:string, Pay:load):command=
        command:
            Subject := Subject
            Target := Target
            Pay := Pay
            AfterEffects := AfterEffects
