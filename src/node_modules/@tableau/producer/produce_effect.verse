using { Tableau }

produce_effector<public> := class(oldeffector):
    ProduceCommand<public>:event(command) = event(command){}

    Play<override>(Command:command, Pay:load):?command=
        ProduceCommand.Signal(Command)

        return option. Command
