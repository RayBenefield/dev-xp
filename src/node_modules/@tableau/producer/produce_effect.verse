using { Tableau }

produce_effector<public> := class(effector):
    ProduceCommand<public>:event(command) = event(command){}

    Play<override>(Command:command, Payload:payload):command=
        ProduceCommand.Signal(Command)

        return Command
