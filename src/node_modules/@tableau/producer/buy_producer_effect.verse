using { Tableau }

buy_producer_effector<public> := class(effector):
    BuyProducerCommand<public>:event(command) = event(command){}

    Play<override>(Command:command, Pay:load):command=
        BuyProducerCommand.Signal(Command)

        return Command
