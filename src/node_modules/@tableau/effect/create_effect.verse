
Create<public><constructor>(Focus:string)<transacts> := effect. Create<constructor>(Path(Focus))
Create<public><constructor>(ResID:string, PropID:string)<transacts> := effect. Create<constructor>(Path(ResID, PropID))
Create<public><constructor>(ResID:half_path, PropID:string)<transacts> := effect. Create<constructor>(Path(ResID, PropID))
Create<public><constructor>(ResID:string, PropID:half_path)<transacts> := effect. Create<constructor>(Path(ResID, PropID))
Create<public><constructor>(ResID:half_path, PropID:half_path)<transacts> := effect. Create<constructor>(Path(ResID, PropID))
Create<public><constructor>(Focus:half_path)<transacts> := effect{ FN := "Create Resource", Focus := Focus }

CreateEffect<public>(Focus:half_path, Tweak:half_path, State:scoped_state)<suspends>:?string=
    NewID := State.ToStr(Focus).Get()
    Type := State.ToStr(Tweak).Get()
    State.CreateResource(NewID, Type)
    return false
