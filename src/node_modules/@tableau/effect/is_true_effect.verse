
IsTrue<public><constructor>(Focus:string)<transacts> := effect. IsTrue<constructor>(Path(Focus))
IsTrue<public><constructor>(ResID:string, PropID:string)<transacts> := effect. IsTrue<constructor>(Path(ResID, PropID))
IsTrue<public><constructor>(ResID:half_path, PropID:string)<transacts> := effect. IsTrue<constructor>(Path(ResID, PropID))
IsTrue<public><constructor>(ResID:string, PropID:half_path)<transacts> := effect. IsTrue<constructor>(Path(ResID, PropID))
IsTrue<public><constructor>(ResID:half_path, PropID:half_path)<transacts> := effect. IsTrue<constructor>(Path(ResID, PropID))
IsTrue<public><constructor>(Focus:half_path)<transacts> := effect{ FN := "Is True", Focus := Focus }

IsTrueEffect<public>(Focus:half_path, Tweak:half_path, State:scoped_state)<suspends>:?string=
    Value := State.ToBool(Focus).Get()
    if (Value?). return false

    State.ExecuteAction(Tweak, EmptyPath())
    return option. "{Focus.GetKey()} is not True"
