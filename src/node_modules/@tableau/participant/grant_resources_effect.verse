using { Tableau }

grant_resources_effector<public> := class(effector):
    AddResourcesCommand<public>:event(command) = event(command){}

    Play<override>(Command:command, Payload:payload):command=
        AddResourcesCommand.Signal(Command)

        return Command
