import minimist from 'minimist';
import kd from '.';

const args = minimist(process.argv.slice(2));
const command = args._[0];

if (!(command in kd)) {
    const msg = `The [${command}] command does not exist in \`kd\``;
    console.log(msg);
    process.exit(1);
}
kd[command](args);
