/* eslint-disable no-console */
import { red } from 'chalk'
import { EMPTY } from 'rxjs'

export default intent => error => {
    console.log(red.bold(`<- [${intent}] Intent Broke ->`))
    console.error(red(error.stack))
    return EMPTY
}
