import { red } from 'chalk'
import { EMPTY } from 'rxjs'
import { log, err } from '@dev-xp/log'

export default intent => error => {
    log(red.bold(`<- [${intent}] Intent Broke ->`))
    err(red(error.stack))
    return EMPTY
}
