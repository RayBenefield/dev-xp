import { map, tap } from 'rxjs/operators'

export default () => pipe => {
    if (pipe.type === 'static') {
        return map(payload => ({
            ...payload,
            context: {
                ...payload.context,
                [pipe.key]: pipe.value,
            },
        }))
    }
    return tap(() => {})
}
