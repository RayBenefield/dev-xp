import { green, grey } from 'chalk'

const success = msg => `    ${green('✔')} ${grey(msg)}`

export const intents = [
    {
        name: 'logTest',
        sources: [{}],
        effects: [{ log: success('Log effects work') }],
    },
    {
        name: 'vote',
        sources: [{}],
        effects: [
            { db: { doc: 'votes', data: { dusty: 1 } } },
            { log: success('Database effects work') },
        ],
    },
    {
        name: 'voteResults',
        sources: [{ db: 'votes' }],
        effects: [
            { db: { doc: 'vote-results', data: { dusty: 1 } } },
            { log: success('Database sources work') },
        ],
    },
]

export const cleanupDocs = ['votes', 'vote-results']
