import * as _ from '@dev-xp/utils'

export const isTemplate = (str = '') => str.startsWith('{') && str.endsWith('}')

export const get = (obj, key) => {
    if (isTemplate(key)) return _.getProp(key.substring(1, key.length - 1), obj)
    return key
}

export default get
