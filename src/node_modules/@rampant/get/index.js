import lodashGet from 'lodash.get'

export const isTemplate = (str = '') => str.startsWith('{') && str.endsWith('}')

export const get = (obj, key) => {
    if (isTemplate(key)) return lodashGet(obj, key.substring(1, key.length - 1))
    return key
}

export default get
