import mock from 'mock-fs';
import describe from 'tape-bdd';
import findPackages from '@kikd/find-packages';

describe('Find Packages', (it) => {
    it('should error for lack of project structure', (assert) => {
        mock({});
        findPackages()
            .catch(() => assert.pass());
        mock.restore();
    });
});
