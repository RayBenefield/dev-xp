import describe from 'tape-bdd';
import withBlueTape from 'blue-tape';
import checkPackages from '@kikd/check-packages';

describe(
    'Check Packages',
    it => {
        it('should error when no arguments are given', assert =>
            checkPackages().catch(() => assert.pass()));
    },
    withBlueTape,
);
